// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Your live site URL (GitHub Pages user site — served at the root, no base path).
  site: 'https://nicolasgarzione.github.io',
});
